// Minimal scenario (traffic only, no injection/expect).

use "../wfs/security.wfs"
use "../wfl/detection_l1.wfl"

#[duration=10m, seed=7]
scenario minimal {
  traffic {
    stream auth_events gen 100/s
  }
}
